<Window x:Class="TelegramBotOnWPF.GetTokenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TelegramBotOnWPF"
        mc:Ignorable="d"
        Title="GetTokenWindow" SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70*"/>
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="TokenText" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Width="400" Height="100"/>
        <Button x:Name="SaveToken" Grid.Row="0" Grid.Column="1" Width="60" Height="50" Content="Сохранить" Click="SaveToken_Click"/>
        <Button x:Name="Cancel" Grid.Row="1" Grid.Column="1" Width="60" Height="50" Content="Выйти" Click="Cancel_Click"/>
    </Grid>
</Window>
